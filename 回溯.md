# 回溯

这篇是我整理回溯题时给自己看的笔记。我的理解是：回溯不是单纯写递归，而是在一棵搜索树上不断做选择、递归、撤销选择。

我做回溯题时最先想的不是代码，而是这三个问题：

当前路径 path 表示什么？
这一层有哪些选择？
什么时候收集答案？

组合：
77 组合
216 组合总和 III
17 电话号码的字母组合
39 组合总和
40 组合总和 II

子集：
78 子集
90 子集 II
491 非递减子序列

排列：
46 全排列
47 全排列 II

切割：
131 分割回文串
93 复原 IP 地址

棋盘：
51 N 皇后
52 N 皇后 II
37 解数独

路径搜索：
79 单词搜索
22 括号生成
332 重新安排行程

## 我的理解

回溯就是一种带撤销的深度优先搜索。

我通常在这些场景里考虑回溯：

枚举所有方案
找出所有组合
找出所有排列
在搜索过程中做选择
选择不合适就撤销

我理解回溯的本质是搜索树。

每一层表示一次选择。
每一条从根到叶子的路径表示一个方案。

## 我怎么判断一道题是不是回溯？

1. 题目是不是要求所有方案？
2. 是否需要在多个选择里不断试？
3. 当前选择会影响后续选择吗？
4. 选错了能不能撤销再试别的？
5. 是否存在明显的组合、排列、子集、棋盘、切割？

所有组合
所有排列
所有子集
所有路径
所有合法括号
所有棋盘摆法

遇到这些要求时，我会先画搜索树，再写递归。

## 我的回溯做题步骤

1. 定义路径 path

path 表示我当前已经做出的选择。

2. 定义选择列表

我当前还能选哪些数？
还能放在哪些位置？
还能切哪些字符串？

3. 定义结束条件

我什么时候得到一个完整答案？

4. 遍历选择

for 循环枚举当前层我可以选的东西。

5. 做选择

把当前选择加入 path。

6. 递归

进入下一层。

7. 撤销选择

递归回来后，把刚才的选择删掉，这一步就是回溯的“撤销”。

## 回溯模板

```c#
void Backtrack(参数) {
    if (结束条件) {
        result.Add(new List<int>(path));
        return;
    }

    for (int i = start; i < 选择列表.Count; i++) {
        if (不合法) continue;

        path.Add(选择);
        Backtrack(下一层参数);
        path.RemoveAt(path.Count - 1);
    }
}
```

我写模板时最关注的是：

递归参数怎么变？
for 从哪里开始？
什么时候收集答案？
怎么去重？

## 组合问题

组合不关心顺序。

比如：

[1, 2] 和 [2, 1] 是同一个组合。

所以要用 startIndex 控制下一层从后面开始选，避免重复。

```c#
IList<IList<int>> Combine(int n, int k) {
    List<IList<int>> result = new List<IList<int>>();
    List<int> path = new List<int>();

    void Backtrack(int start) {
        if (path.Count == k) {
            result.Add(new List<int>(path));
            return;
        }

        for (int i = start; i <= n; i++) {
            path.Add(i);
            Backtrack(i + 1);
            path.RemoveAt(path.Count - 1);
        }
    }

    Backtrack(1);
    return result;
}
```

剪枝：

如果剩下的数不够选，就不用继续了。

```c#
for (int i = start; i <= n - (k - path.Count) + 1; i++)
```

## 子集问题

子集问题和组合很像。

区别是：

每个节点都是一个答案。

不是只有叶子节点才是答案。

```c#
IList<IList<int>> Subsets(int[] nums) {
    List<IList<int>> result = new List<IList<int>>();
    List<int> path = new List<int>();

    void Backtrack(int start) {
        result.Add(new List<int>(path));

        for (int i = start; i < nums.Length; i++) {
            path.Add(nums[i]);
            Backtrack(i + 1);
            path.RemoveAt(path.Count - 1);
        }
    }

    Backtrack(0);
    return result;
}
```

## 排列问题

排列关心顺序。

比如：

[1, 2] 和 [2, 1] 是不同排列。

所以每一层都可以从 0 开始遍历。

但一个元素不能重复使用，所以需要 used 数组。

```c#
IList<IList<int>> Permute(int[] nums) {
    List<IList<int>> result = new List<IList<int>>();
    List<int> path = new List<int>();
    bool[] used = new bool[nums.Length];

    void Backtrack() {
        if (path.Count == nums.Length) {
            result.Add(new List<int>(path));
            return;
        }

        for (int i = 0; i < nums.Length; i++) {
            if (used[i]) continue;

            used[i] = true;
            path.Add(nums[i]);

            Backtrack();

            path.RemoveAt(path.Count - 1);
            used[i] = false;
        }
    }

    Backtrack();
    return result;
}
```

## 去重问题

我写回溯时，去重是最容易混乱的地方。

先排序。

然后判断同一层是否用过相同元素。

组合去重：

```c#
Array.Sort(candidates);

for (int i = start; i < candidates.Length; i++) {
    if (i > start && candidates[i] == candidates[i - 1]) {
        continue;
    }

    path.Add(candidates[i]);
    Backtrack(i + 1);
    path.RemoveAt(path.Count - 1);
}
```

排列去重：

```c#
Array.Sort(nums);

for (int i = 0; i < nums.Length; i++) {
    if (used[i]) continue;

    if (i > 0 && nums[i] == nums[i - 1] && !used[i - 1]) {
        continue;
    }

    used[i] = true;
    path.Add(nums[i]);
    Backtrack();
    path.RemoveAt(path.Count - 1);
    used[i] = false;
}
```

我给自己的记法：

同一层不能重复选相同数字。
同一条路径可以根据题目决定能不能重复使用。

## 切割问题

切割问题也是组合问题。

比如分割回文串：

每次选择一个切割位置。
如果当前子串是回文，就继续切后面的。

```c#
void Backtrack(string s, int start) {
    if (start == s.Length) {
        result.Add(new List<string>(path));
        return;
    }

    for (int i = start; i < s.Length; i++) {
        if (!IsPalindrome(s, start, i)) continue;

        path.Add(s.Substring(start, i - start + 1));
        Backtrack(s, i + 1);
        path.RemoveAt(path.Count - 1);
    }
}
```

## 棋盘问题

棋盘题通常是一行一行放。

以 N 皇后为例：

第 row 行放一个皇后。
遍历每一列 col。
如果当前位置合法，就放。
然后递归下一行。
回来后撤销。

判断是否合法：

同一列不能有皇后。
左上对角线不能有皇后。
右上对角线不能有皇后。

## 我写回溯最容易错的点

1. 忘记撤销选择

path.Add 之后，递归回来一定要 Remove。

2. result 里直接加 path

错误：

result.Add(path)

这样后面 path 改了，答案也会跟着变。

要写：

result.Add(new List<int>(path))

3. startIndex 用错

组合要 startIndex。
排列一般要 used。
子集也要 startIndex。

4. 去重条件写错

去重前要先排序。

组合去重看 i > start。
排列去重看 used[i - 1]。

5. 终止条件和收集答案混淆

组合：一般满足长度才收集。
子集：每个节点都收集。
排列：path 长度等于 nums.Length 才收集。
切割：start 到字符串末尾才收集。

## 我的总结

回溯题我现在会先画搜索树，再写代码。只要能想清楚“每一层选什么”“path 表示什么”“什么时候收集答案”，代码基本就顺了。

我最需要警惕的是去重和撤销选择。很多回溯题不是难在递归，而是难在同一层去重、同一路径去重、元素能不能重复使用这几个细节。
