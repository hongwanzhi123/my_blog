# 图论（搜索）

这篇是我整理图论搜索时的笔记。我的理解是：很多题表面上是矩阵、字符串、课程安排，真正做题时都可以抽象成“点”和“边”。

我做这类题时会先判断：

状态能不能看成点？
一次合法移动能不能看成边？
题目要的是连通性、可达性，还是最短步数？

DFS / BFS 基础：
200 岛屿数量
695 岛屿的最大面积
463 岛屿的周长
130 被围绕的区域
417 太平洋大西洋水流问题

网格搜索：
733 图像渲染
994 腐烂的橘子
542 01 矩阵
1091 二进制矩阵中的最短路径
79 单词搜索

图遍历：
797 所有可能的路径
841 钥匙和房间
547 省份数量
133 克隆图

拓扑排序：
207 课程表
210 课程表 II
802 找到最终的安全状态

最短路：
127 单词接龙
752 打开转盘锁
743 网络延迟时间
787 K 站中转内最便宜的航班

并查集：
684 冗余连接
990 等式方程的可满足性
1319 连通网络的操作次数

## 我的理解

图是由点和边组成的结构。

很多题看起来不是图，但我会尝试把它抽象成图：

网格里的每个格子是点。
相邻格子之间有边。
字符串每次变化一个字符是一条边。
课程依赖关系是一条有向边。

我理解图论搜索的核心就是：

从一个点出发
沿着边访问其它点
避免重复访问
根据题目统计答案

## 我怎么判断一道题是不是图搜索？

1. 题目里是否有连通、可达、路径、最短距离？
2. 是否能把状态看成点，把状态转移看成边？
3. 是否需要从一个起点扩散到周围？
4. 是否有网格上下左右移动？
5. 是否有依赖关系或者先后顺序？

岛屿数量
腐烂的橘子
课程表
单词接龙
打开转盘锁

遇到这些关键词时，我会优先考虑 DFS、BFS、拓扑排序或者并查集。

## 我的图搜索做题步骤

1. 建图或隐式建图

有些题给了 edges，我会显式建图。
有些题不需要真的建图，比如网格题直接用方向数组。

2. 确定搜索方式

DFS：我一般用于连通块、遍历所有可能路径。
BFS：我一般用于最短路、按层扩散。

3. 定义 visited

visited 用来避免重复访问，也避免死循环。

4. 定义起点

从单个点开始？
还是多个点同时开始？

5. 处理答案

统计连通块数量。
统计面积。
记录层数。
判断是否有环。

## DFS

我理解 DFS 是一条路走到底，再回头换路。

适合：

连通块
岛屿面积
所有路径
回溯搜索

网格 DFS 模板：

```c#
int[][] dirs = new int[][] {
    new int[] { 1, 0 },
    new int[] { -1, 0 },
    new int[] { 0, 1 },
    new int[] { 0, -1 }
};

void DFS(char[][] grid, int r, int c) {
    if (r < 0 || r >= grid.Length || c < 0 || c >= grid[0].Length) {
        return;
    }

    if (grid[r][c] != '1') return;

    grid[r][c] = '0';

    foreach (var dir in dirs) {
        DFS(grid, r + dir[0], c + dir[1]);
    }
}
```

岛屿数量：

```c#
int NumIslands(char[][] grid) {
    int count = 0;

    for (int i = 0; i < grid.Length; i++) {
        for (int j = 0; j < grid[0].Length; j++) {
            if (grid[i][j] == '1') {
                count++;
                DFS(grid, i, j);
            }
        }
    }

    return count;
}
```

## BFS

我理解 BFS 是按层扩散。

适合：

最短路径
最少步数
多源扩散
层序遍历

网格 BFS 模板：

```c#
int BFS(int[][] grid, int startR, int startC) {
    Queue<(int r, int c)> queue = new Queue<(int r, int c)>();
    bool[,] visited = new bool[grid.Length, grid[0].Length];

    queue.Enqueue((startR, startC));
    visited[startR, startC] = true;

    int step = 0;

    while (queue.Count > 0) {
        int size = queue.Count;

        for (int i = 0; i < size; i++) {
            var cur = queue.Dequeue();

            foreach (var dir in dirs) {
                int nr = cur.r + dir[0];
                int nc = cur.c + dir[1];

                if (nr < 0 || nr >= grid.Length || nc < 0 || nc >= grid[0].Length) continue;
                if (visited[nr, nc]) continue;

                visited[nr, nc] = true;
                queue.Enqueue((nr, nc));
            }
        }

        step++;
    }

    return step;
}
```

## 多源 BFS

我理解多源 BFS，就是把多个起点同时放进队列。

适合：

腐烂的橘子
01 矩阵
地图最高点

腐烂的橘子：

一开始所有腐烂橘子都是起点。
每一轮向外扩散。
层数就是分钟数。

```c#
Queue<(int r, int c)> queue = new Queue<(int r, int c)>();

for (int i = 0; i < grid.Length; i++) {
    for (int j = 0; j < grid[0].Length; j++) {
        if (grid[i][j] == 2) {
            queue.Enqueue((i, j));
        }
    }
}
```

注意：

多源 BFS 不是从每个起点分别 BFS。
而是所有起点一起入队。

## 拓扑排序

我把拓扑排序理解成“按依赖关系排队”。

它用于有向无环图。

适合解决依赖关系。

比如课程表：

学习课程 A 之前必须先学课程 B。
这就是一条边：

B -> A

拓扑排序思路：

统计每个点的入度。
把入度为 0 的点入队。
每取出一个点，就删除它指向的边。
如果最后能取出所有点，说明没有环。

```c#
bool CanFinish(int numCourses, int[][] prerequisites) {
    List<int>[] graph = new List<int>[numCourses];
    int[] indegree = new int[numCourses];

    for (int i = 0; i < numCourses; i++) {
        graph[i] = new List<int>();
    }

    foreach (var edge in prerequisites) {
        int course = edge[0];
        int pre = edge[1];

        graph[pre].Add(course);
        indegree[course]++;
    }

    Queue<int> queue = new Queue<int>();
    for (int i = 0; i < numCourses; i++) {
        if (indegree[i] == 0) queue.Enqueue(i);
    }

    int count = 0;

    while (queue.Count > 0) {
        int cur = queue.Dequeue();
        count++;

        foreach (int next in graph[cur]) {
            indegree[next]--;
            if (indegree[next] == 0) queue.Enqueue(next);
        }
    }

    return count == numCourses;
}
```

## 最短路

无权图最短路：

用 BFS。

每条边权重一样，第一次到达某个点就是最短距离。

有权图最短路：

常见是 Dijkstra。

如果边权可能为负，就不能直接用 Dijkstra。

算法题里常见：

网格最短路：BFS
单词接龙：BFS
网络延迟时间：Dijkstra
K 站中转：动态规划 / Bellman-Ford 思想

## 并查集

我一般用并查集处理连通性。

支持两个操作：

Find：找到一个点所属集合的代表。
Union：合并两个集合。

```c#
class UnionFind {
    int[] parent;

    public UnionFind(int n) {
        parent = new int[n];
        for (int i = 0; i < n; i++) {
            parent[i] = i;
        }
    }

    public int Find(int x) {
        if (parent[x] != x) {
            parent[x] = Find(parent[x]);
        }
        return parent[x];
    }

    public bool Union(int a, int b) {
        int rootA = Find(a);
        int rootB = Find(b);

        if (rootA == rootB) return false;

        parent[rootA] = rootB;
        return true;
    }
}
```

适合：

判断两个点是否连通
统计连通块数量
检测无向图是否成环

## visited 的使用

我使用 visited 的目的：

避免重复访问
避免死循环
保证 BFS 最短路正确

DFS 网格题可以直接修改原数组：

grid[r][c] = '0'

也可以单独开 visited。

BFS 通常入队时就标记 visited。

不要等出队时再标记，否则同一个点可能被重复入队。

## 我写图论搜索最容易错的点

1. 忘记 visited

图可能有环。

不加 visited 就可能无限递归或重复入队。

2. BFS 层数算错

如果题目问最少步数，要明确：

初始状态算第 0 步？
还是第 1 步？

3. 多源 BFS 写成多次 BFS

多源 BFS 要一次性把所有起点入队。

4. 网格边界写错

r < 0
r >= rows
c < 0
c >= cols

四个条件缺一不可。

5. 拓扑排序边方向建反

课程表里：

[a, b] 表示学 a 前要先学 b。

所以边是：

b -> a

6. DFS 爆栈

网格很大、递归很深时，DFS 可能栈溢出。

这时可以改成 BFS 或手动栈。

## 我的总结

图论搜索题我会先把题目翻译成“点、边、起点、终点、访问规则”。如果题目问连通块，我优先想 DFS 或并查集；如果题目问最短步数，我优先想 BFS；如果题目有依赖关系，我优先想拓扑排序。

我觉得这类题最重要的是不要被题面包装迷惑。网格、单词、课程、密码锁，本质上都可以是图。
