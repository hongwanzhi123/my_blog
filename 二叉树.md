# 二叉树

遍历基础：
144 二叉树的前序遍历
94 二叉树的中序遍历
145 二叉树的后序遍历
102 二叉树的层序遍历
107 二叉树的层序遍历 II
103 二叉树的锯齿形层序遍历

深度 / 高度：
104 二叉树的最大深度
111 二叉树的最小深度
110 平衡二叉树
543 二叉树的直径

路径问题：
112 路径总和
113 路径总和 II
124 二叉树中的最大路径和
257 二叉树的所有路径
437 路径总和 III

构造 / 修改：
226 翻转二叉树
105 从前序与中序遍历序列构造二叉树
106 从中序与后序遍历序列构造二叉树
654 最大二叉树
114 二叉树展开为链表

公共祖先：
236 二叉树的最近公共祖先
235 二叉搜索树的最近公共祖先

二叉搜索树：
98 验证二叉搜索树
700 二叉搜索树中的搜索
701 二叉搜索树中的插入操作
450 删除二叉搜索树中的节点
530 二叉搜索树的最小绝对差
501 二叉搜索树中的众数
538 把二叉搜索树转换为累加树

## 概念

二叉树题的核心不是“树”，而是“递归结构”。

每个节点都可以看成一棵树的根：

当前节点
左子树
右子树

所以很多二叉树问题，本质都是：

当前节点要做什么？
左子树返回什么？
右子树返回什么？
当前节点怎么合并左右子树的信息？

二叉树天然适合递归，因为它的结构本身就是递归定义的。

## 怎么判断一道题是不是二叉树递归？

1. 题目问的是整棵树的某种性质吗？
2. 当前节点的答案能不能由左右子树推出？
3. 是否需要遍历每个节点？
4. 是否存在“从根到叶子”或者“任意路径”的要求？
5. 是否需要在回溯时把子树信息带回父节点？

最大深度
是否平衡
路径总和
最近公共祖先
翻转二叉树
验证搜索树

这些大概率都可以用递归。

## 二叉树做题步骤

1. 明确遍历顺序
   前序：先处理当前节点，再处理左右子树。
   中序：先左子树，再当前节点，再右子树。
   后序：先左右子树，再处理当前节点。
   层序：按层从上到下处理。

2. 明确递归函数含义
   这个函数输入什么？
   返回什么？
   它负责解决哪一棵子树的问题？

3. 写终止条件
   一般是 root == null。

4. 处理当前节点
   根据题目决定是在递归前处理，递归中处理，还是递归后处理。

5. 合并左右子树结果
   比如 max、sum、&&、路径拼接、区间判断。

## 二叉树的遍历

1. 前序遍历

顺序：

根
左
右

适合：

复制一棵树
序列化
先处理当前节点再往下传状态

```c#
void Preorder(TreeNode root) {
    if (root == null) return;

    Visit(root);
    Preorder(root.left);
    Preorder(root.right);
}
```

2. 中序遍历

顺序：

左
根
右

适合：

二叉搜索树
因为 BST 中序遍历是递增序列。

```c#
void Inorder(TreeNode root) {
    if (root == null) return;

    Inorder(root.left);
    Visit(root);
    Inorder(root.right);
}
```

3. 后序遍历

顺序：

左
右
根

适合：

需要先知道左右子树结果，再决定当前节点答案。

比如：

树的高度
是否平衡
最大路径和
删除节点

```c#
int Depth(TreeNode root) {
    if (root == null) return 0;

    int left = Depth(root.left);
    int right = Depth(root.right);

    return Math.Max(left, right) + 1;
}
```

## 层序遍历

层序遍历就是 BFS。

每一层的节点一起处理，适合求：

最小深度
每层平均值
每层最大值
右视图
锯齿形遍历

```c#
IList<IList<int>> LevelOrder(TreeNode root) {
    List<IList<int>> ans = new List<IList<int>>();
    if (root == null) return ans;

    Queue<TreeNode> queue = new Queue<TreeNode>();
    queue.Enqueue(root);

    while (queue.Count > 0) {
        int size = queue.Count;
        List<int> level = new List<int>();

        for (int i = 0; i < size; i++) {
            TreeNode node = queue.Dequeue();
            level.Add(node.val);

            if (node.left != null) queue.Enqueue(node.left);
            if (node.right != null) queue.Enqueue(node.right);
        }

        ans.Add(level);
    }

    return ans;
}
```

## 路径问题

路径问题通常有两类：

1. 从根到叶子

比如路径总和：

路径必须从 root 开始，到 leaf 结束。

```c#
bool HasPathSum(TreeNode root, int targetSum) {
    if (root == null) return false;

    if (root.left == null && root.right == null) {
        return targetSum == root.val;
    }

    return HasPathSum(root.left, targetSum - root.val)
        || HasPathSum(root.right, targetSum - root.val);
}
```

2. 任意路径

比如二叉树最大路径和：

路径可以从任意节点开始，到任意节点结束。

这种题一般需要：

递归返回“给父节点用的贡献值”
全局变量维护“最终答案”

```c#
int ans = int.MinValue;

int MaxGain(TreeNode root) {
    if (root == null) return 0;

    int left = Math.Max(0, MaxGain(root.left));
    int right = Math.Max(0, MaxGain(root.right));

    ans = Math.Max(ans, root.val + left + right);

    return root.val + Math.Max(left, right);
}
```

注意：

返回给父节点时，只能选一边。
更新答案时，可以同时选左右两边。

## 二叉搜索树

BST 的性质：

左子树所有节点 < 当前节点
右子树所有节点 > 当前节点
左右子树也必须是 BST

BST 题最常用的是中序遍历。

因为 BST 的中序遍历是递增序列。

验证 BST：

```c#
long prev = long.MinValue;

bool IsValidBST(TreeNode root) {
    if (root == null) return true;

    if (!IsValidBST(root.left)) return false;

    if (root.val <= prev) return false;
    prev = root.val;

    return IsValidBST(root.right);
}
```

## 最近公共祖先

二叉树最近公共祖先的核心：

如果 p 和 q 分别在当前节点左右两边，那么当前节点就是答案。

```c#
TreeNode LowestCommonAncestor(TreeNode root, TreeNode p, TreeNode q) {
    if (root == null || root == p || root == q) {
        return root;
    }

    TreeNode left = LowestCommonAncestor(root.left, p, q);
    TreeNode right = LowestCommonAncestor(root.right, p, q);

    if (left != null && right != null) return root;
    if (left != null) return left;
    return right;
}
```

## 二叉树最容易错的点

1. 递归函数含义不清

比如返回的是：

当前子树高度？
当前节点贡献值？
当前子树是否满足条件？
当前路径是否存在？

这些含义必须一开始就定清楚。

2. 空节点处理错

最大深度里 null 返回 0。
验证 BST 里 null 返回 true。
路径总和里 null 不能直接代表找到路径。

不同题目里 null 的含义不一样。

3. 前序和后序用错

需要从上往下传状态，一般前序。
需要从下往上收集信息，一般后序。

4. BST 只比较当前左右孩子

错误写法：

root.left.val < root.val
root.right.val > root.val

这是不够的。

BST 要保证整棵左子树都小于 root，整棵右子树都大于 root。

5. 路径返回值和全局答案混淆

最大路径和这类题：

返回值是给父节点用的。
全局答案是整棵树的最优。

这两个不是一个东西。
