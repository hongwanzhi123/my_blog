# 枚举

基础枚举：
1 两数之和
15 三数之和
18 四数之和
11 盛最多水的容器
42 接雨水

子数组 / 子串：
560 和为 K 的子数组
209 长度最小的子数组
3 无重复字符的最长子串
438 找到字符串中所有字母异位词

区间枚举：
56 合并区间
57 插入区间
435 无重叠区间
452 用最少数量的箭引爆气球

状态枚举：
78 子集
90 子集 II
784 字母大小写全排列
1239 串联字符串的最大长度

矩阵枚举：
54 螺旋矩阵
59 螺旋矩阵 II
73 矩阵置零
48 旋转图像

优化枚举：
167 两数之和 II
16 最接近的三数之和
713 乘积小于 K 的子数组
1248 统计优美子数组

## 概念

枚举就是把所有可能情况都试一遍。

最朴素的想法往往是枚举。

但算法题里的枚举不是无脑暴力，而是：

枚举什么？
怎么避免重复？
怎么减少无效枚举？
能不能用数据结构加速判断？
能不能利用单调性优化？

枚举是很多复杂算法的起点。

## 怎么判断一道题可以先从枚举想？

1. 题目问是否存在某种组合？
2. 题目问满足条件的数量？
3. 题目数据范围比较小？
4. 可以把答案拆成若干个选择吗？
5. 是否能固定一部分，再快速求另一部分？

两数之和
三数之和
子数组数量
区间交叠
状态选择

这些都可以先想枚举。

## 枚举做题步骤

1. 确定枚举对象

枚举下标？
枚举值？
枚举左端点？
枚举右端点？
枚举状态？

2. 确定枚举顺序

从前往后？
从小到大？
固定一个，再移动另一个？

3. 写出暴力复杂度

O(n)
O(n^2)
O(n^3)
O(2^n)

4. 根据数据范围判断能不能过

n = 100，可以 O(n^3)
n = 10^3，可以 O(n^2)
n = 10^5，一般要 O(n log n) 或 O(n)

5. 优化枚举

哈希表
排序 + 双指针
前缀和
滑动窗口
二分
剪枝

## 两数之和

暴力枚举：

```c#
int[] TwoSum(int[] nums, int target) {
    for (int i = 0; i < nums.Length; i++) {
        for (int j = i + 1; j < nums.Length; j++) {
            if (nums[i] + nums[j] == target) {
                return new int[] { i, j };
            }
        }
    }

    return new int[0];
}
```

时间复杂度：

O(n^2)

优化：

固定当前数 nums[i]。
要找 target - nums[i] 是否出现过。

用哈希表记录之前的数。

```c#
int[] TwoSum(int[] nums, int target) {
    Dictionary<int, int> map = new Dictionary<int, int>();

    for (int i = 0; i < nums.Length; i++) {
        int need = target - nums[i];

        if (map.ContainsKey(need)) {
            return new int[] { map[need], i };
        }

        map[nums[i]] = i;
    }

    return new int[0];
}
```

时间复杂度：

O(n)

## 三数之和

三数之和的思路：

先排序。
固定一个数 nums[i]。
剩下两个数用双指针。

```c#
IList<IList<int>> ThreeSum(int[] nums) {
    Array.Sort(nums);
    List<IList<int>> ans = new List<IList<int>>();

    for (int i = 0; i < nums.Length; i++) {
        if (i > 0 && nums[i] == nums[i - 1]) continue;

        int left = i + 1;
        int right = nums.Length - 1;

        while (left < right) {
            int sum = nums[i] + nums[left] + nums[right];

            if (sum == 0) {
                ans.Add(new List<int> { nums[i], nums[left], nums[right] });

                while (left < right && nums[left] == nums[left + 1]) left++;
                while (left < right && nums[right] == nums[right - 1]) right--;

                left++;
                right--;
            } else if (sum < 0) {
                left++;
            } else {
                right--;
            }
        }
    }

    return ans;
}
```

核心：

固定一个数
把两数之和变成双指针
排序后才能去重和移动指针

## 子数组枚举

子数组常见枚举方式：

枚举左端点和右端点。

```c#
for (int left = 0; left < n; left++) {
    int sum = 0;
    for (int right = left; right < n; right++) {
        sum += nums[right];
    }
}
```

时间复杂度：

O(n^2)

如果每次重新求和，就是 O(n^3)。

所以至少要在枚举右端点时顺手维护 sum。

## 前缀和优化枚举

如果题目问：

子数组和等于 k 的数量

暴力枚举所有区间是 O(n^2)。

可以用前缀和优化：

sum[i] - sum[j] = k

也就是：

sum[j] = sum[i] - k

```c#
int SubarraySum(int[] nums, int k) {
    Dictionary<int, int> map = new Dictionary<int, int>();
    map[0] = 1;

    int sum = 0;
    int ans = 0;

    foreach (int num in nums) {
        sum += num;

        if (map.ContainsKey(sum - k)) {
            ans += map[sum - k];
        }

        if (!map.ContainsKey(sum)) map[sum] = 0;
        map[sum]++;
    }

    return ans;
}
```

## 滑动窗口优化枚举

当数组元素都是正数，或者窗口满足单调性时，可以用滑动窗口。

比如：

长度最小的子数组

```c#
int MinSubArrayLen(int target, int[] nums) {
    int left = 0;
    int sum = 0;
    int ans = int.MaxValue;

    for (int right = 0; right < nums.Length; right++) {
        sum += nums[right];

        while (sum >= target) {
            ans = Math.Min(ans, right - left + 1);
            sum -= nums[left];
            left++;
        }
    }

    return ans == int.MaxValue ? 0 : ans;
}
```

窗口题的核心：

right 负责扩张。
left 负责收缩。
每个元素最多进出窗口一次。

时间复杂度：

O(n)

## 状态枚举

如果 n 很小，比如 n <= 20，可以考虑用二进制枚举子集。

每一位表示选不选。

```c#
for (int mask = 0; mask < (1 << n); mask++) {
    for (int i = 0; i < n; i++) {
        if ((mask & (1 << i)) != 0) {
            // 第 i 个元素被选中
        }
    }
}
```

复杂度：

O(n * 2^n)

适合：

枚举子集
状态压缩
小规模选择问题

## 枚举最容易错的点

1. 重复枚举

比如三数之和要去重。

固定 i 后，如果 nums[i] == nums[i - 1]，就跳过。

2. 边界错

双循环里：

j 从 i + 1 开始？
还是从 i 开始？

区间是 [left, right]？
还是 [left, right)？

3. 复杂度估错

n = 10^5 时，O(n^2) 通常过不了。

4. 滑动窗口乱用

滑动窗口需要单调性。

如果数组里有负数，sum 增减不再单调，很多窗口写法就不成立。

5. 去重位置错

去重通常发生在同一层枚举。

三数之和里：

i 要去重。
left 和 right 找到答案后也要去重。
