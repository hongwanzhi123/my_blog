# 分治

基础分治：
704 二分查找
69 x 的平方根
50 Pow(x, n)
169 多数元素
53 最大子数组和

排序相关：
912 排序数组
148 排序链表
215 数组中的第 K 个最大元素
23 合并 K 个升序链表

区间分治：
241 为运算表达式设计优先级
95 不同的二叉搜索树 II
108 将有序数组转换为二叉搜索树
105 从前序与中序遍历序列构造二叉树
106 从中序与后序遍历序列构造二叉树

归并思想：
88 合并两个有序数组
21 合并两个有序链表
315 计算右侧小于当前元素的个数
493 翻转对
327 区间和的个数

快速选择：
215 数组中的第 K 个最大元素
347 前 K 个高频元素
973 最接近原点的 K 个点

## 概念

分治就是：

把一个大问题拆成几个规模更小、形式相同的问题。
先解决小问题。
再把小问题的答案合并成大问题的答案。

核心是三步：

分解
解决
合并

如果一个问题可以不断拆成左右两部分，并且左右两部分的答案能合并，那么就可以考虑分治。

## 怎么判断一道题是不是分治？

1. 大问题能不能拆成几个相似的小问题？
2. 小问题之间能不能相对独立求解？
3. 小问题的答案能不能合并？
4. 题目是否有明显的区间、左右、前后、递归结构？
5. 是否可以用中点、基准值、根节点把问题切开？

数组排序
链表排序
二分查找
快速选择
合并 K 个链表
根据遍历序列构造二叉树

这些都常见分治思想。

## 分治做题步骤

1. 明确函数定义

这个函数解决哪个范围的问题？

比如：

Sort(nums, left, right)
表示排序 nums[left..right]

2. 写递归终止条件

范围为空
范围只有一个元素
节点为空

3. 拆分问题

通常按中点拆：

mid = left + (right - left) / 2

也可能按基准值拆：

pivot

4. 分别解决子问题

递归处理左边和右边。

5. 合并答案

归并排序要 merge。
最大子数组和要合并跨中点的答案。
构造二叉树要连接左右子树。

## 分治模板

```c#
Result Divide(Problem problem) {
    if (problem is small enough) {
        return SolveDirectly(problem);
    }

    Problem left = GetLeftProblem(problem);
    Problem right = GetRightProblem(problem);

    Result leftAns = Divide(left);
    Result rightAns = Divide(right);

    return Merge(leftAns, rightAns);
}
```

重点不是模板本身，而是：

子问题怎么划分？
返回值是什么？
怎么合并？

## 二分查找

二分是最常见的分治。

它每次把搜索范围缩小一半。

适用条件：

数组有序
答案具有单调性

```c#
int BinarySearch(int[] nums, int target) {
    int left = 0;
    int right = nums.Length - 1;

    while (left <= right) {
        int mid = left + (right - left) / 2;

        if (nums[mid] == target) return mid;
        if (nums[mid] < target) left = mid + 1;
        else right = mid - 1;
    }

    return -1;
}
```

## 归并排序

归并排序是典型分治：

把数组分成两半
分别排序
合并两个有序数组

```c#
void MergeSort(int[] nums, int left, int right) {
    if (left >= right) return;

    int mid = left + (right - left) / 2;

    MergeSort(nums, left, mid);
    MergeSort(nums, mid + 1, right);

    Merge(nums, left, mid, right);
}
```

合并：

```c#
void Merge(int[] nums, int left, int mid, int right) {
    int[] temp = new int[right - left + 1];
    int i = left;
    int j = mid + 1;
    int k = 0;

    while (i <= mid && j <= right) {
        if (nums[i] <= nums[j]) temp[k++] = nums[i++];
        else temp[k++] = nums[j++];
    }

    while (i <= mid) temp[k++] = nums[i++];
    while (j <= right) temp[k++] = nums[j++];

    for (int p = 0; p < temp.Length; p++) {
        nums[left + p] = temp[p];
    }
}
```

时间复杂度：

O(n log n)

空间复杂度：

O(n)

## 快速排序

快速排序也是分治：

选一个 pivot
把小于 pivot 的放左边
大于 pivot 的放右边
再递归排序左右两边

```c#
void QuickSort(int[] nums, int left, int right) {
    if (left >= right) return;

    int p = Partition(nums, left, right);

    QuickSort(nums, left, p - 1);
    QuickSort(nums, p + 1, right);
}
```

快速排序的重点是 partition。

```c#
int Partition(int[] nums, int left, int right) {
    int pivot = nums[right];
    int i = left;

    for (int j = left; j < right; j++) {
        if (nums[j] <= pivot) {
            Swap(nums, i, j);
            i++;
        }
    }

    Swap(nums, i, right);
    return i;
}
```

平均时间复杂度：

O(n log n)

最坏时间复杂度：

O(n^2)

## 快速选择

快速选择用于求第 K 大 / 第 K 小。

它和快排类似，但每次只递归一边。

```c#
int FindKthLargest(int[] nums, int k) {
    int target = nums.Length - k;
    int left = 0;
    int right = nums.Length - 1;

    while (left <= right) {
        int p = Partition(nums, left, right);

        if (p == target) return nums[p];
        if (p < target) left = p + 1;
        else right = p - 1;
    }

    return -1;
}
```

平均时间复杂度：

O(n)

## 合并 K 个有序链表

这题也可以用分治：

先合并左半部分链表
再合并右半部分链表
最后合并两个链表

```c#
ListNode MergeKLists(ListNode[] lists) {
    if (lists == null || lists.Length == 0) return null;
    return Divide(lists, 0, lists.Length - 1);
}

ListNode Divide(ListNode[] lists, int left, int right) {
    if (left == right) return lists[left];

    int mid = left + (right - left) / 2;
    ListNode l1 = Divide(lists, left, mid);
    ListNode l2 = Divide(lists, mid + 1, right);

    return MergeTwoLists(l1, l2);
}
```

## 分治最容易错的点

1. 递归边界错

left >= right
left == right
left > right

这些含义不同。

排序数组一般 left >= right 返回。
构造树时 left > right 返回 null。

2. mid 写溢出

不要写：

(left + right) / 2

更常用：

left + (right - left) / 2

3. 合并逻辑不清

分治最难的通常不是递归，而是 merge。

比如：

归并排序怎么合并？
最大子数组和怎么合并跨中点？
构造树怎么接左右子树？

4. 快排 partition 死循环

双指针 partition 要特别注意等于 pivot 的情况。

如果指针不动，就可能死循环。

5. 忘记分治不一定比直接做更简单

有些题虽然能拆，但合并成本太高。

如果合并成本很大，分治就未必划算。
